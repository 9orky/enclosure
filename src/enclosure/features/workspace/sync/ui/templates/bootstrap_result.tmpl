{% set target_dir = result.target_dir|present_path(project_root) %}
{% if result.created_dir %}Created{% else %}Found{% endif %} {{ target_dir }}.
{% if result.created_files %}
Created {{ result.created_files|length }} file(s).
{% for path in result.created_files %}
- {{ path|present_path(project_root) }}
{% endfor %}
{% endif %}
{% if result.preserved_files %}
Preserved {{ result.preserved_files|length }} existing file(s).
{% endif %}
Local profile surface: {{ target_dir }}/rules/local/.
Safe to rerun: plain 'enclosure' preserves existing local files in {{ target_dir }}/rules/local/.
Next step: review {{ target_dir }}/enclosure.yaml and {{ target_dir }}/rules/local/ and run 'enclosure architecture boundaries'.
